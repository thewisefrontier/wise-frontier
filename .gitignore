# 환경변수 - 절대 GitHub에 올리면 안 됨
.env

# 내부 운영 노트 - Private 여부와 무관하게 git에 올리지 않음(로컬 전용)
notes/

# 상태 파일

# 로그
logs/

# Python 캐시
__pycache__/
*.pyc
*.pyo

# OS 파일
.DS_Store
Thumbs.db

# Claude Code 개인 설정(권한 규칙 등) - 사람마다 다를 수 있어 커밋 안 함
.claude/settings.local.json

# GA4 서비스 계정 키(로컬 테스트용, CI는 GA4_SERVICE_ACCOUNT_JSON 시크릿)
ga4-key.json
