name: DB ?”í˜¸??ë°±ì—…

on:
  # GitHub ?ì²´ schedule: ?¸ë¦¬ê±°ëŠ” ???€?¥ì†Œ?ì„œ ë°˜ë³µ?ìœ¼ë¡?ë¬´ì‘??ì§€??
  # ?¬ê³ ë¥????„ë?ê°€ ?ˆì–´ ?¤ë¥¸ ?Œí¬?Œë¡œ?€ ?™ì¼?˜ê²Œ ?¸ë? ?¬ë¡ (cron-job.org)??
  # workflow_dispatchë¡??€ì²´í•œ??2026-09-03 ?ê? ?????Œí¬?Œë¡œë§??›ë‚ 
  # schedule: ê·¸ë?ë¡??¨ì•„?ˆì—ˆ??.
  #
  # 2026-09-23: ë§¤ì¼ ??ì£?1???¼ìš”??ë¡?ë³€ê²??¬ìš©??ê²°ì •). Supabase ë¬´ë£Œ ?Œëœ
  # ?´ê·¸?ˆìŠ¤ ?œë„(5GB) ì´ˆê³¼ë¡?? ì˜ˆê¸°ê°„ ì§„ì… ??pg_dump??ë§¤ë²ˆ DB ?„ì²´ë¥?ë°–ìœ¼ë¡?
  # ë¹¼ë‚´(?•ì¶• ???˜ë°± MB) ë§¤ì¼?´ë©´ ?´ê²ƒë§Œìœ¼ë¡??œë„???¡ë°•?œë‹¤. ê·¸ë‚  ?¤í”„??
  # IO ?¤ë¡œ?€ ?íƒœ?ì„œ 71ë¶?ê±¸ë¦¬ë©??¬ì´?¸ê¹Œì§€ ?ë ¤ì§€ê²??ˆë‹¤. cron-job.org??
  # ê·¸ë?ë¡?ë§¤ì¼ ?¸ì¶œ?˜ê³ , ?¬ê¸°???”ì¼ë¡?ê±°ë¥¸?? ê¸‰í•˜ê²??°ë¡œ ë°±ì—…???„ìš”?˜ë©´
  # ?˜ë™ ?¤í–‰ ??force=true.
  workflow_dispatch:
    inputs:
      force:
        description: '?”ì¼ê³?ë¬´ê??˜ê²Œ ì§€ê¸?ë°±ì—… ?¤í–‰'
        required: false
        default: 'false'

concurrency:
  group: db-backup
  cancel-in-progress: false

jobs:
  gate:
    runs-on: ubuntu-latest
    outputs:
      run: ${{ steps.day.outputs.run }}
    steps:
      - id: day
        env:
          FORCE: ${{ inputs.force }}
        run: |
          if [ "$FORCE" = "true" ] || [ "$(date -u +%u)" = "7" ]; then echo "run=true" >> "$GITHUB_OUTPUT"
          else echo "run=false" >> "$GITHUB_OUTPUT"; echo "?¼ìš”???„ë‹˜ ??ì£?1??ë°±ì—…?´ë¼ ê±´ë„ˆ?€"; fi

  backup:
    needs: gate
    if: needs.gate.outputs.run == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: PostgreSQL 17 ?´ë¼?´ì–¸???¤ì¹˜
        run: |
          sudo install -d /usr/share/postgresql-common/pgdg
          sudo curl -sSf -o /usr/share/postgresql-common/pgdg/apt.postgresql.org.asc https://www.postgresql.org/media/keys/ACCC4CF8.asc
          echo "deb [signed-by=/usr/share/postgresql-common/pgdg/apt.postgresql.org.asc] https://apt.postgresql.org/pub/repos/apt $(lsb_release -cs)-pgdg main" | sudo tee /etc/apt/sources.list.d/pgdg.list
          sudo apt-get update -qq
          sudo apt-get install -y -qq postgresql-client-17

      - name: DB ?¤í”„ (Session Pooler, IPv4)
        env:
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          pg_dump -h aws-1-ap-northeast-2.pooler.supabase.com -p 5432 \
            -U postgres.fotdngseksqaghvtcvqh -d postgres \
            --no-owner --no-privileges -F c -f backup.dump

      - name: ?”í˜¸??(AES-256)
        run: |
          openssl enc -aes-256-cbc -pbkdf2 -salt \
            -in backup.dump \
            -out "newsfinal-$(date +%Y%m%d).dump.enc" \
            -pass env:BACKUP_PASSWORD
          rm backup.dump
        env:
          BACKUP_PASSWORD: ${{ secrets.BACKUP_PASSWORD }}

      # ë°±ì—… ?€?¥ì†Œ: Cloudflare R2 ë¹„ê³µê°?ë²„í‚·. ???€?¥ì†Œ??public?´ë¼ Actions ?„í‹°?©íŠ¸??
      # GitHub ê³„ì •ë§??ˆìœ¼ë©??„êµ¬???´ë ¤ë°›ì„ ???ˆë‹¤(2026-09-22 ?ê?) ???„ë¡¬?„íŠ¸Â·?¤ì • ?Œì´ë¸”ì´ ??
      # ?¤í”„ë¥?ê·¸ë ‡ê²??????†ì–´ ??¸´?? ë²„í‚·?€ ê³µê°œ ?‘ê·¼/ì»¤ìŠ¤?€ ?„ë©”?¸ì„ ì¼œì? ?ŠëŠ” ??ë¹„ê³µê°?
      # ?œê³„: wrangler object put?€ ?Œì¼ 1ê°???300MB ?í•œ(?„ì¬ ?¤í”„ ??130MB) ???˜ìœ¼ë©?S3 ë©€?°íŒŒ?¸ë¡œ ?„í™˜.
      - name: R2 ë²„í‚· ì¤€ë¹?(ë¹„ê³µê°? 30??ë§Œë£Œ)
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          npx --yes wrangler@4 r2 bucket info newsfinal-backups >/dev/null 2>&1 || npx --yes wrangler@4 r2 bucket create newsfinal-backups
          npx --yes wrangler@4 r2 bucket lifecycle list newsfinal-backups 2>&1 | grep -q expire-30d || npx --yes wrangler@4 r2 bucket lifecycle add newsfinal-backups expire-30d "" --expire-days 30 --force

      - name: R2 ?…ë¡œ??+ ë¬´ê²°???€ì¡?
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          f=$(ls newsfinal-*.dump.enc)
          npx --yes wrangler@4 r2 object put "newsfinal-backups/$f" --file "$f" --remote
          npx --yes wrangler@4 r2 object get "newsfinal-backups/$f" --file "roundtrip.enc" --remote
          cmp "$f" roundtrip.enc && echo "R2 ?•ë³µ ?€ì¡??¼ì¹˜: $(stat -c %s "$f") bytes"
          rm -f roundtrip.enc


      # ìµœì¢… ë°±ì—…: êµ¬ê? ?œë¼?´ë¸Œ(? ë£Œ ?©ëŸ‰ 600GB+). ì§€?°ì? ?Šê³  ?êµ¬ ë³´ê??œë‹¤(2026-09-22 ?¬ìš©??ê²°ì •) ??
      # R2??30??ì§€?˜ë©´ ?ë™ ?? œ?˜ëŠ” ?„ì‹œ ?¬ë³¸?´ê³ , ?œë¼?´ë¸Œê°€ ë§ˆì?ë§?ë³´ë£¨?? ???¨ê³„?ëŠ” ?? œ ëª…ë ¹???£ì? ë§?ê²?
      # ?œí¬ë¦?RCLONE_DRIVE_TOKEN(rclone authorize ì¶œë ¥) ?ëŠ” RCLONE_CONFIG(rclone.conf ?„ì²´, remote ?´ë¦„ gdrive)ê°€ ?†ìœ¼ë©?ê±´ë„ˆ?´ë‹¤.
      # ? ï¸ ?œí¬ë¦¿ì„ RCLONE_* ?´ë¦„???˜ê²½ë³€?˜ë¡œ ?˜ê¸°ì§€ ë§?ê²???rclone??ê·??´ë¦„???ê¸° ?µì…˜(?? --drive-token)?¼ë¡œ ?½ì–´ ?¤ë¥˜ê°€ ?œë‹¤.
      # ìµœì¢… ë°±ì—…?´ë¼ ?¤íŒ¨?˜ë©´ ?¤í–‰??ë¹¨ê°›ê²??¤íŒ¨?œí‚¨??ê²€ì¦??¨ê³„??if: always()ë¡?ê·¸ë˜???ˆë‹¤).
      - name: êµ¬ê? ?œë¼?´ë¸Œ ìµœì¢… ë°±ì—… (?êµ¬ ë³´ê?)
        env:
          GDRIVE_CONFIG_DATA: ${{ secrets.RCLONE_CONFIG }}
          GDRIVE_TOKEN_DATA: ${{ secrets.RCLONE_DRIVE_TOKEN }}
        run: |
          if [ -z "$GDRIVE_CONFIG_DATA" ] && [ -z "$GDRIVE_TOKEN_DATA" ]; then echo "RCLONE_DRIVE_TOKEN/RCLONE_CONFIG ?œí¬ë¦??†ìŒ - ?œë¼?´ë¸Œ ë³µì‚¬ ê±´ë„ˆ?€"; exit 0; fi
          sudo apt-get install -y -qq rclone
          mkdir -p ~/.config/rclone
          if [ -n "$GDRIVE_CONFIG_DATA" ]; then printf '%s' "$GDRIVE_CONFIG_DATA" > ~/.config/rclone/rclone.conf
          else
            # rclone authorize ì¶œë ¥?€ ??ê°€ì§€ ?•íƒœ: ? í° JSON({...}) ?ëŠ” base64 ë¬¶ìŒ(client_id/secret/token). ????ë°›ëŠ”??
            python3 - <<'PY'
          import os, json, base64
          v = os.environ["GDRIVE_TOKEN_DATA"].strip()
          conf = ["[gdrive]", "type = drive", "scope = drive.file"]
          if v.startswith("{"):
              tok = v
          else:
              b = v + "=" * (-len(v) % 4)
              try: d = json.loads(base64.urlsafe_b64decode(b))
              except Exception: d = json.loads(base64.b64decode(b))
              tok = d["token"] if isinstance(d["token"], str) else json.dumps(d["token"])
              for k in ("client_id", "client_secret"):
                  if d.get(k): conf.append(f"{k} = {d[k]}")
          conf.append("token = " + tok)
          open(os.path.expanduser("~/.config/rclone/rclone.conf"), "w").write("\n".join(conf) + "\n")
          PY
          fi
          f=$(ls newsfinal-*.dump.enc)
          rclone copyto "$f" "gdrive:newsfinal-backups/$f"
          rclone lsl "gdrive:newsfinal-backups/$f"   # copytoê°€ ?…ë¡œ?????´ì‹œë¥??€ì¡°í•œ??
          rclone size gdrive:newsfinal-backups

      # R2 ?…ë¡œ???¤ì— ê²€ì¦í•œ?? ???¨ê³„ê°€ ?˜ëª»?¼ë„ ë°±ì—… ?ì²´???´ë? ?€?¥ë¼ ?ˆë‹¤.
      # ë³µí˜¸?????„ì¹´?´ë¸Œ ëª©ì°¨ ?½ê¸° ???µì‹¬ ?Œì´ë¸??°ì´??ì¡´ì¬ ?•ì¸. ê¹¨ì§„/ë¹??¤í”„ë©??¤í–‰??ë¹¨ê°›ê²??¤íŒ¨?œë‹¤.
      - name: ë³µêµ¬ ê°€??ê²€ì¦?(ë³µí˜¸??+ ëª©ì°¨ ?•ì¸)
        if: always()
        env:
          BACKUP_PASSWORD: ${{ secrets.BACKUP_PASSWORD }}
        run: |
          openssl enc -d -aes-256-cbc -pbkdf2 -in newsfinal-*.dump.enc -out verify.dump -pass env:BACKUP_PASSWORD
          /usr/lib/postgresql/17/bin/pg_restore -l verify.dump > toc.txt
          echo "TOC ??ª© ?? $(wc -l < toc.txt)"
          for t in articles prompts user_roles article_translations; do
            grep -Eq "TABLE DATA public $t( |$)" toc.txt || { echo "::error::$t ?Œì´ë¸??°ì´?°ê? ?¤í”„???†ìŒ"; exit 1; }
          done
          rm -f verify.dump toc.txt
