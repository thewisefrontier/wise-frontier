name: NewsFinal - Entity Verification

on:
  # GitHub ?ì²´ schedule: ?¸ë¦¬ê±°ëŠ” ???€?¥ì†Œ?ì„œ ë°˜ë³µ?ìœ¼ë¡?ë¬´ì‘??ì§€??
  # ?¬ê³ ë¥????„ë?ê°€ ?ˆì–´ ?¤ë¥¸ ?Œí¬?Œë¡œ?€ ?™ì¼?˜ê²Œ ?¸ë? ?¬ë¡ (cron-job.org)??
  # workflow_dispatchë¡??€ì²´í•œ??2026-09-03 ?ê? ?????Œí¬?Œë¡œë§??›ë‚ 
  # schedule: ê·¸ë?ë¡??¨ì•„?ˆì—ˆ??. ?ë˜ ì£¼ê¸°: ë§¤ì¼ UTC 21:30 = KST 06:30
  # (?Œì—­ ?ê? 06:00 ?¤ìŒ).
  workflow_dispatch:

jobs:
  verify:
    runs-on: ubuntu-latest

    steps:
      - name: ì½”ë“œ ì²´í¬?„ì›ƒ
        uses: actions/checkout@v5

      - name: Python ?¤ì •
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: ?¨í‚¤ì§€ ?¤ì¹˜
        run: pip install -r requirements.txt

      - name: ê³ ìœ ëª…ì‚¬(?‘í’ˆëª…Â·ì¸ëª…Â·ì?ëª??? ?¤ë¥˜ ?ê?
        run: python scripts/verify_entities.py
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEY_2: ${{ secrets.GEMINI_API_KEY_2 }}
          GEMINI_API_KEY_3: ${{ secrets.GEMINI_API_KEY_3 }}
          GEMINI_API_KEY_4: ${{ secrets.GEMINI_API_KEY_4 }}
          GEMINI_API_KEY_5: ${{ secrets.GEMINI_API_KEY_5 }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          AIVEN_SERVICE_URI: ${{ secrets.AIVEN_SERVICE_URI }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_ADMIN_CHAT_ID: ${{ secrets.TELEGRAM_ADMIN_CHAT_ID }}
          NVIDIA_API_KEY_L: ${{ secrets.NVIDIA_API_KEY_L }}
