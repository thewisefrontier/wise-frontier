name: One-Off Maintenance Task

# ?˜ë™ ?¤í–‰ ?„ìš©
on:
  workflow_dispatch:
    inputs:
      script:
        description: '?¤í–‰???¤í¬ë¦½íŠ¸ ?Œì¼ëª?(scripts/ ?´ë” ê¸°ì?, ?? one_off/backfill_companies.py)'
        required: true
        default: 'one_off/backfill_companies.py'

permissions:
  contents: read

jobs:
  run_script:
    runs-on: ubuntu-latest

    steps:
      - name: ì½”ë“œ ì²´í¬?„ì›ƒ
        uses: actions/checkout@v5

      - name: Python ?¤ì •
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: ?¨í‚¤ì§€ ?¤ì¹˜
        run: pip install -r requirements.txt

      - name: ?¤í¬ë¦½íŠ¸ ?¤í–‰
        # 2026-09-03 ë³´ì•ˆ ?ê?: ?Œí¬?Œë¡œ ?…ë ¥ê°’ì„ run: ?¸ì— ì§ì ‘ ì¹˜í™˜?˜ë©´
        # script ?…ë ¥????ë©”í?ë¬¸ìë¥??£ì–´ ?„ì˜ ëª…ë ¹ ?¤í–‰??ê°€?¥í–ˆ??GitHub
        # ê³µì‹ ë¬¸ì„œê°€ ê²½ê³ ?˜ëŠ” ?¤í¬ë¦½íŠ¸ ?¸ì ???ˆí‹°?¨í„´ ????job?€ ëª¨ë“ 
        # ?œí¬ë¦¿ì„ envë¡?ê°–ê³  ?ˆì–´ ?„í—˜??ì»¸ë‹¤). envë¡?ë°›ì•„ ?Œì¼ëª??¨í„´??
        # ê²€ì¦í•œ ?¤ì—ë§??¤í–‰?œë‹¤.
        run: |
          echo "?¤í–‰: scripts/${SCRIPT}"
          if [[ ! "$SCRIPT" =~ ^[A-Za-z0-9_./-]+\.py$ ]]; then
            echo "::error::?ˆìš©?˜ì? ?ŠëŠ” ?Œì¼ëª??•ì‹: $SCRIPT"
            exit 1
          fi
          python "scripts/${SCRIPT}"
        env:
          SCRIPT: ${{ inputs.script }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_ADMIN_CHAT_ID: ${{ secrets.TELEGRAM_ADMIN_CHAT_ID }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEY_2: ${{ secrets.GEMINI_API_KEY_2 }}
          GEMINI_API_KEY_3: ${{ secrets.GEMINI_API_KEY_3 }}
          GEMINI_API_KEY_4: ${{ secrets.GEMINI_API_KEY_4 }}
          GEMINI_API_KEY_5: ${{ secrets.GEMINI_API_KEY_5 }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          AIVEN_SERVICE_URI: ${{ secrets.AIVEN_SERVICE_URI }}
          SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
          KMA_API_KEY: ${{ secrets.KMA_API_KEY }}
          KMA_BRIEFING_KEY: ${{ secrets.KMA_BRIEFING_KEY }}
