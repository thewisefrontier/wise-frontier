name: NewsFinal - Economic Indicators Update

on:
  # GitHub ?ì²´ schedule: ?¸ë¦¬ê±°ëŠ” ???€?¥ì†Œ?ì„œ ë°˜ë³µ?ìœ¼ë¡?ë¬´ì‘??ì§€??
  # ?¬ê³ ë¥????„ë?ê°€ ?ˆì–´ ?¤ë¥¸ ?Œí¬?Œë¡œ?€ ?™ì¼?˜ê²Œ ?¸ë? ?¬ë¡ (cron-job.org)??
  # workflow_dispatchë¡??€ì²´í•œ??2026-09-03 ?ê? ?????Œí¬?Œë¡œë§??›ë‚ 
  # schedule: ê·¸ë?ë¡??¨ì•„?ˆì—ˆ??. ?ë˜ ì£¼ê¸°: ë§¤ë…„ 4??1?¼Â?0??1??KST 10:00
  # (IMF WEO ë´?ê°€??ë°œí‘œ ì§í›„).
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch_indicators:
    runs-on: ubuntu-latest

    steps:
      - name: ì½”ë“œ ì²´í¬?„ì›ƒ
        uses: actions/checkout@v5

      - name: Python ?¤ì •
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: ?¨í‚¤ì§€ ?¤ì¹˜
        run: pip install requests python-dotenv

      - name: IMF ê²½ì œì§€???˜ì§‘
        run: python scripts/fetch_econ_indicators.py

      - name: ë³€ê²½ì‚¬??ì»¤ë°‹ ë°??¸ì‹œ
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git add -A
          git diff --staged --quiet || git commit -m "Update economic indicators (IMF WEO)"
          for i in 1 2 3 4 5; do
            git pull origin main -X ours --no-rebase
            git push && break
            echo "push failed (attempt $i) - another workflow pushed first, retrying"
            sleep $((RANDOM % 8 + 3))
          done
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
