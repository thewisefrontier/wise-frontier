name: NewsFinal - Backfill Value-Add

on:
  # 2026-08-03: 백필 100% ?�료(4,275/4,275)�?cron 비활?�화.
  # ?�개가 ?�요?�면 ?�래 2줄의 주석???�제??�?
  # schedule:
  #   - cron: '10,40 * * * *'   # 매시 10분�?0�?(run.yml 47�? weather.yml 20,50분과 겹치지 ?�게 ?�프??
  workflow_dispatch:

concurrency:
  group: backfill-value-add
  cancel-in-progress: false

jobs:
  backfill:
    runs-on: ubuntu-latest

    steps:
      - name: 코드 체크?�웃
        uses: actions/checkout@v5

      - name: Python ?�정
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'

      - name: ?�키지 ?�치
        run: pip install -r requirements.txt

      - name: 3줄요???�자?�이?�어 백필
        run: python scripts/backfill_value_add.py
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_API_KEY_2: ${{ secrets.GEMINI_API_KEY_2 }}
          GEMINI_API_KEY_3: ${{ secrets.GEMINI_API_KEY_3 }}
          GEMINI_API_KEY_4: ${{ secrets.GEMINI_API_KEY_4 }}
          GEMINI_API_KEY_5: ${{ secrets.GEMINI_API_KEY_5 }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
          AIVEN_SERVICE_URI: ${{ secrets.AIVEN_SERVICE_URI }}
