# NewsFinal 시스템 아키텍처 현황 및 통합 로드맵
*작성일: 2026-07-28*

## 1. 현황 — 파편화 구조

현재 시스템은 기능별로 독립 스크립트가 난립해 있어, 공통 로직(Gemini 호출, 합쇼체 감지·변환, DB 저장, 프롬프트 로딩 등)이 파일마다 중복 구현된 상태다. 하나를 수정해도 나머지에 반영되지 않는 구조적 문제가 있다.

---

## 2. 전체 파이프라인 지도

### 2-1. GitHub Actions 워크플로우

| 워크플로우 | 스케줄 | 실행 스크립트 | 비고 |
|---|---|---|---|
| `run.yml` | 매시 정각 | `rss_fetcher.py` → `gemini_summarizer.py` → `gemini_writer.py` | 메인 파이프라인 |
| `weather.yml` | 매시 20분 | `weather_report.py` → `export_articles.py` | |
| `oil_price.yml` | 평일 UTC 22:30 / 00:00 | `oil_price_writer.py` → `export_articles.py` | |
| `digest.yml` | UTC 20:00 (KST 05:00) | `daily_digest.py` → `export_articles.py` | |
| `briefing.yml` | UTC 08:00 (KST 17:00) | `briefing.py` → `export_articles.py` | |
| `auto-dedup.yml` | 수동 | `auto_dedup.py` → `export_articles.py` | |
| `backfill_value_add.yml` | 매시 10분·40분 | `backfill_value_add.py` | 백필 완료 후 비활성화 예정 |
| `transliteration_check.yml` | UTC 21:00 (KST 06:00) | `check_transliteration.py` | |
| `econ_calendar.yml` | 매월 1일 | `econ_calendar_fetch.py` | |
| `econ_indicators.yml` | 매년 4월·10월 1일 | `fetch_econ_indicators.py` | |
| `db-backup.yml` | 매일 UTC 18:00 (KST 03:00) | — | DB 백업 |
| `deploy-worker.yml` | 수동 | — | Cloudflare Worker 배포 |
| `one_off_task.yml` | 수동 | `inputs.script` 지정 | 일회성 작업용 |

### 2-2. Supabase Edge Functions

| 함수 | 버전 | 역할 |
|---|---|---|
| `merge-articles` | v9 | 기사 병합 처리 (Gemini 호출 포함, 독자적 Gemini 로직) |

### 2-3. Cloudflare Functions

| 파일 | 역할 |
|---|---|
| `functions/article.js` | `/article?id=X` 엣지 SSR — 크롤러용 완성 HTML 반환 |
| `functions/hello.js` | 테스트용 |

---

## 3. 스크립트별 역할 및 파편화 현황

| 스크립트 | 역할 | Gemini 호출 | DB 저장 | 합쇼체 방어 | 독립도 |
|---|---|---|---|---|---|
| `gemini_writer.py` | 메인 기사 생성 | ✅ (자체 완결) | ✅ | ✅ 3단 완비 | **완성형** |
| `gemini_summarizer.py` | 트렌드 3함수 요약 | ✅ (자체 완결) | ✅ | ✅ 3단 완비 | 독립 |
| `weather_report.py` | 날씨 기사 생성 | ✅ (자체 완결) | ✅ | ❌ 미적용 | 독립 |
| `oil_price_writer.py` | 유가 기사 생성 | ✅ (자체 완결) | ✅ | ❌ 미적용 | 독립 |
| `daily_digest.py` | 다이제스트 생성 | ✅ (자체 완결) | ✅ | ❌ 미적용 | 독립 |
| `econ_writer.py` | 경제지표 기사 생성 | ✅ (자체 완결) | ✅ | ❌ 미적용 | 독립, **미가동** |
| `rss_fetcher.py` | RSS 수집 | ❌ | ✅ | — | 독립 |
| `external_trends.py` | 트렌드 감지 | ✅ (Gemini 선별) | ✅ | — | 독립 |
| `export_articles.py` | Cloudflare 배포 동기화 | ❌ | ❌ | — | 독립 |
| `auto_dedup.py` | 중복 기사 처리 | ❌ | ✅ | — | 독립 |
| `backfill_value_add.py` | summary_3lines·investment_idea 백필 | ✅ | ✅ | — | 독립 |
| `check_transliteration.py` | 음역 오류 자동 교정 | ❌ | ✅ | — | 독립 |
| `briefing.py` | 텔레그램 브리핑 | ❌ | ❌ | — | 독립 |
| `db.py` | DB 헬퍼 | ❌ | — | — | 공유 가능 (미활용) |
| `econ_calendar_fetch.py` | 경제지표 일정 수집 | ❌ | ✅ | — | 독립 |
| `fetch_econ_indicators.py` | 거시경제 지표 수집 | ❌ | ✅ | — | 독립 |

---

## 4. 핵심 문제

1. **공통 로직 중복**: Gemini 호출·키 로테이션·합쇼체 변환·DB 저장·프롬프트 로딩이 파일마다 별도 구현
2. **수정 파급 불가**: 한 파일 버그 수정이 다른 파일에 자동 반영되지 않음 (예: 이번 합쇼체 방어 → writer·summarizer만 적용, 나머지 미적용)
3. **`merge-articles` Edge Function**: 서버리스 환경이라 Python 스크립트와 코드 공유 자체가 불가. 별도 관리 불가피
4. **`db.py` 미활용**: 공유 DB 헬퍼가 있으나 각 스크립트가 독자적으로 requests 호출

---

## 5. 통합 방향

### 원칙
- `gemini_writer.py`는 **완성형, 건드리지 않음**
- 신규 모듈은 `gemini_writer.py`를 import해서 공통 함수 재사용
- 기존 모듈 소급 리팩토링은 리스크 대비 실익 없어 **점진적으로만** 진행

### 신규 모듈 작성 패턴
`gemini_writer.py`가 `if __name__ == "__main__": run()` 구조라 import 시 실행되지 않음. 바로 사용 가능:

```python
# 신규 스크립트 예시 (travel_guide_updater.py 등)
from gemini_writer import (
    call_gemini,
    call_gemini_article,
    has_polite_ending,
    to_plain_style,
    load_prompt,
    _ensure_paragraphs,
    _sb_headers,
    SUPABASE_URL,
)
```

### 중장기 통합 로드맵 (우선순위 순)

| 단계 | 대상 | 방법 | 시점 |
|---|---|---|---|
| 1 | 신규 모듈 (`travel_guide_updater.py` 등) | `gemini_writer` import | 신규 작성 시마다 |
| 2 | `weather_report.py` | 합쇼체 방어 이식 (날씨 기사 정책 확정 후) | 정책 결정 후 |
| 3 | `oil_price_writer.py`, `daily_digest.py` | 합쇼체 방어 이식 또는 import 전환 | 여유 생길 때 |
| 4 | `external_trends.py`, `rss_fetcher.py` | Gemini 호출부 import 전환 | 장기 |
| 5 | 공통 모듈(`core.py`) 분리 | 전체 스크립트 리팩토링 | 대규모 개편 시 |

---

## 6. 합쇼체 방어 현황 (2026-07-28 기준)

### 적용 완료
- `gemini_writer.py`: 3단 방어
  1. writer_rules v15 프롬프트 지시
  2. `call_gemini_article()` — 합쇼체 감지 시 재생성 1회
  3. `_plainify_parsed()` — 모든 파싱 경로(JSON/레거시/라이브 업데이트) 통과, 강제 변환
- `gemini_summarizer.py`: 동일 3단 방어 + `has_polite_ending()` 버그 수정

### 미적용
- `weather_report.py` — 날씨 기사 합쇼체 81건, 의도된 톤인지 정책 미결
- `oil_price_writer.py`, `daily_digest.py`, `econ_writer.py` — 미적용

### 공통 수정 사항 (2026-07-28)
- `has_polite_ending()`을 `to_plain_style(text) != text` 방식으로 변경
  - 기존: `습니다/입니다/됩니다`만 탐지 → `개최합니다.` / `아닙니다.` 누락
  - 변경 후: 변환기가 실제로 고칠 수 있는 패턴과 정확히 일치

---

## 7. 미결 사항

- 날씨 기사 합쇼체 81건: 수정 대상인지 의도된 톤인지 정책 결정 필요
- `econ_writer.py`: 미가동 상태, 활성화 계획 미정
- `backfill_value_add.py`: 진행 중 (2026-07-28 기준 ~11.7%), 완료 후 워크플로우 비활성화 필요
- 여행 정보 Stage 4 (`travel_guide_updater.py`): 미착수, 신규 작성 시 `gemini_writer` import 구조로 작성 예정
